/**
 * Staff attendance screen — re-uses the operator attendance log view.
 * Staff can mark and view daily tenant attendance without financial access.
 */
export { default } from '../(operator)/attendance-log';
