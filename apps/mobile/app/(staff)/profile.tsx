/**
 * Staff profile screen — re-uses the operator profile.
 * Shows role as "Staff", displays name/email, and has sign-out button.
 * No financial summary (global summary will be empty or minimal for staff).
 */
export { default } from '../(operator)/profile';
