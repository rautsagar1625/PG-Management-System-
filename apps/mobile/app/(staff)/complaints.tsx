/**
 * Staff complaints screen — same as operator but accessed from the restricted
 * staff route group. Staff can view and update complaint status without seeing
 * financial data (collections, leads, settlements) visible in the operator group.
 */
export { default } from '../(operator)/complaints';
